<!DOCTYPE html>
<html lang="es">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{{ app.name }}</title>
  </head>
  <body style="margin:0;padding:0;background-color:#f7f7f7;font-family:Arial,Helvetica,sans-serif;color:#111827;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="background:#f7f7f7;padding:24px 0;">
      <tr>
        <td align="center">
          <table role="presentation" width="600" cellspacing="0" cellpadding="0" border="0" style="background:#ffffff;border-radius:8px;overflow:hidden;">
            <tr>
              <td style="background: {{ app.primary_color }};padding:16px 24px;color:#fff;">
                {% if app.logo_url %}
                  <img src="{{ app.logo_url }}" alt="{{ app.name }}" style="max-height:32px;display:block;"/>
                {% else %}
                  <strong style="font-size:18px;">{{ app.name }}</strong>
                {% endif %}
              </td>
            </tr>
            <tr>
              <td style="padding:24px;">
                {{ content | safe }}
              </td>
            </tr>
            <tr>
              <td style="padding:16px 24px;font-size:12px;color:#6b7280;border-top:1px solid #e5e7eb;">
                © {{ app.name }}
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
  </html>

