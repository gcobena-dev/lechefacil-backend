DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/lechefacil
TENANT_HEADER=X-Tenant-ID
LOG_LEVEL=INFO
ENVIRONMENT=dev
JWT_SECRET_KEY=changeme-super-secret
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=60
JWT_ISSUER=https://lechefacil.local
JWT_AUDIENCE=lechefacil-users

# Storage / images (dev defaults)
S3_BUCKET=bucket-name
S3_REGION=us-east-1
S3_PREFIX=prefix/
S3_PUBLIC_URL_BASE=
IMAGE_THUMB_SIZES=256,64 
MAX_IMAGE_BYTES=10485760

# CORS (comma-separated list)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Email settings
EMAIL_PROVIDER=logging
EMAIL_FROM_NAME=LecheFacil
EMAIL_FROM_ADDRESS=no-reply@lechefacil.local
EMAIL_ADMIN_RECIPIENTS=["admin2@dominio.com"]

# SMTP (Gmail) example — requires 2FA + App Password
# EMAIL_PROVIDER=smtp
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_account@gmail.com
# SMTP_PASSWORD=your_16_char_app_password
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false
# EMAIL_FROM_ADDRESS=your_account@gmail.com
# EMAIL_FROM_NAME=LecheFacil
# EMAIL_ADMIN_RECIPIENTS=["admin@yourdomain.com"]
